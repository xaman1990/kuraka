# Pattern Detector - Required Rules

Read these before analyzing patterns:

1. `sie_v2/.claude/CLAUDE.md` - Project conventions (to understand which patterns violate rules)
2. `sie_v2/.claude/rules/16-agent-backup.md` - Backup sync rules

Also read:
- `docs/process/agent-retrospectives/RETRO-*.md` — ALL retros (main data source)
- `docs/process/patterns/RECURRING-ISSUES.md` — previous patterns report (if exists, to avoid duplicates)

Do NOT read code rules (01-15) — the pattern detector reviews process data, not code.
